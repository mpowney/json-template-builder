import { Route, Routes, useParams } from "react-router-dom";

export const DefinedRoutes: React.FunctionComponent = () => {

    const { ocId } = useParams();
    return (
        <></>
    )

}

export default DefinedRoutes;